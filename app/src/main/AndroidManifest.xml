<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FCMilan"
        tools:targetApi="31">
        <activity
            android:name=".activities.GalleryActivity"
            android:exported="false"
            android:label="@string/title_activity_gallery"
            android:theme="@style/Theme.FCMilan" />
        <activity
            android:name=".activities.MatchesActivity"
            android:exported="false"
            android:label="@string/title_activity_matches"
            android:theme="@style/Theme.FCMilan" />
        <activity
            android:name=".activities.PlayersActivity"
            android:exported="false"
            android:label="@string/title_activity_players"
            android:theme="@style/Theme.FCMilan" />
        <activity
            android:name=".activities.TrophiesActivity"
            android:exported="false"
            android:label="@string/title_activity_trophies"
            android:theme="@style/Theme.FCMilan" />
        <activity
            android:name=".activities.HistoryActivity"
            android:exported="false"
            android:label="@string/title_activity_history"
            android:theme="@style/Theme.FCMilan" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FCMilan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>